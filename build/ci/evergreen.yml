exec_timeout_secs: 4200
stepback: true
command_type: system
pre_error_fails_task: true
include:
  - filename: build/ci/papertrail.yml

tasks:
  - name: no_op
  - run_on:
      - ubuntu2404-small
    commands:
      - command: shell.exec
        script: |
          echo "This is a no-op, does nothing."
