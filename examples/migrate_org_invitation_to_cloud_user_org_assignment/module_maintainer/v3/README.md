# module_maintainer/v3 (final)

Final module definition after migration is complete. The `moved` block from v2 has been removed since all users have migrated.

The module now uses `mongodbatlas_cloud_user_org_assignment` and `mongodbatlas_cloud_user_team_assignment` resources.

