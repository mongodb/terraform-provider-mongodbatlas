# v3: Cleaned Up Configuration

State: Final module consumption after migration is complete. 
- Only `mongodbatlas_cloud_user_org_assignment` remains.
The `import` blocks from v2 have been removed since migration has been applied.

