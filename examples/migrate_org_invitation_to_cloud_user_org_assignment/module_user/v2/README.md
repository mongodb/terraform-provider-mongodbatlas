# v2 Module Usage Example: Cloud User Org Assignment

This example demonstrates how to use the `mongodbatlas_cloud_user_org_assignment` resource within a Terraform module. It shows how to assign a user to an org using module inputs.
